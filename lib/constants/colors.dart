import 'package:flutter/material.dart';

const Color bgColorLight = Color(0x0000000f);
const Color primaryColorLight = Color(0xffffffff);

const Color bgColorDark = Color(0xffffffff);
const Color primaryColorDark = Color(0x00000004);